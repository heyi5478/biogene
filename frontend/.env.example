# Base URL of the backend gateway. Consumed by src/lib/api.ts via
# `import.meta.env.VITE_API_BASE_URL`. Local dev default lives in
# `.env.development`; copy this file to `.env.local` to override per machine.
VITE_API_BASE_URL=http://localhost:8000
